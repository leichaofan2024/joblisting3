  <tr>
    <td><%= link_to(job.title, admin_job_path(job)) %></td>
    <td>
      <%= link_to("编辑", edit_admin_job_path(job),class: "btn btn-primary") %>
      <%= link_to("删除", admin_job_path(job),class: "btn btn-danger", :method => :delete, :data => {:confirm => "Are you sure?"}) %>
    </td>
    <td>
       <%= job.created_at %>
    </td>
  </tr>
